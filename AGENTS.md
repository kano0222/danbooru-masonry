# Danbooru Masonry 协作约定

## 目标与授权

- 以用户明确的目标为边界，优先正确性、可维护性和最小充分改动。可以反驳需求，但应说明事实、影响及更合适的做法，不盲从，也不擅自改变目标。
- 区分讨论与执行。观点、猜测和方案讨论不自动授权改动；明确要求实现或修改后，自主完成目标内必要的工作，不为常规实现选择反复确认。
- 只读检查、已授权范围内的可回滚编辑和相关验证可自主进行。提交、打标签，以及改变远端、第三方服务或用户持久化数据的操作，必须在明确授权范围内；本地文件编辑可由实现或修改请求授权，但不能据此推导出推送、发布、对外评论、修改远端配置或删除用户数据的授权。
- 已有授权持续有效，不要求用户逐步重复确认；授权范围不清且下一步涉及上述副作用时，只问最少必要问题。

## 执行方式

- 修改前检查工作树及相关实现，保留用户已有改动。复用现有结构和工具链，不顺带重构、格式化或扩展功能。
- 自主选择实现和验证方式。优先解决当前实际问题，不为假设风险增加评分体系、审批流程、配置项或额外架构；必要的复杂度应有具体依据。
- 优先使用已有依赖。新增依赖需说明必要性；涉及新的外部服务、费用或权限时先确认。
- 验证与改动风险相称，使用现有 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 等命令；纯文档修改检查内容和差异即可。失败后修复并重跑相关检查，不机械重复已通过且未受影响的检查。
- 网络请求应能在单元测试中替换或 mock，测试不依赖在线 Danbooru 或翻译 CDN。
- UI 修改关注实际涉及的交互状态、键盘可用性、点击区域和宿主 CSS 影响。必要时在真实页面验证；用户测试结果可作为证据，未验证的效果如实说明。

## 内容与一致性

- 文档直接说明用途、用法、数据来源和实际处理方式，保留必要事实，删除冗余；不写营销表达、空泛评价或无助于使用的实现细节。
- 处理界面提示、说明和设置项文案时，先判断其是否帮助用户理解或操作。删除内部实现、重复信息和容易引起误解的说明；仅在信息必要但表达不清时精简改写。目标是减少阅读负担，不为保留原文而重述无实际价值的信息。
- 改动涉及文档、设置或界面行为时，相关中英文 README、默认值、界面文字和测试应与当前实现一致，不作无关联动。致谢只表达感谢，许可措辞放在相应说明中。
- 回复先给结论，只说明实际改动、关键依据、验证结果和影响当前决策的限制。方案给出足以执行的内容，不为凑完整性罗列无关风险或后续工作。

## 发布约束

- 不自行决定版本号。发布准备同步 `package.json`、`vite.config.ts` 和 CHANGELOG；更新日志只写该版本的用户可见变化，使用实际发布日期。
- 发布前检查仓库现有发布脚本的覆盖范围，运行发布验证并补充未覆盖的必要测试和 `git diff --check`；避免重复执行已覆盖且未受影响的检查。核对构件元数据和版本一致性，相关检查失败时不发布。
- 经授权提交或推送时，只包含目标内的文件和提交。`dist/`、原始数据库、缓存和临时文件不得提交。
- 发布提交使用 `release: vX.Y.Z`，标签指向对应提交，不覆盖已有标签。推送标签会触发 GitHub Release；完成后核对工作流、更新日志和脚本附件。
